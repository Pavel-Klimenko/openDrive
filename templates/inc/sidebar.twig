<div class="col-12 col-lg-3">
    <div class="card">
        <div class="card-body">
            {% if response.current_path != '/basket/' %}
                {{ include('inc/sidebar_blocks/disk.twig') }}
            {% else %}
                {{ include('inc/sidebar_blocks/basket.twig') }}
            {% endif %}
        </div>
    </div>

    {{ render(controller(
        'App\\Controller\\EmbeddingController::getUserDiskInfo'
    )) }}
</div>
